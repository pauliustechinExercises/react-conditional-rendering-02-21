const Test = () => {
  return (
    <div className="border">
      <p>Basic Conditional Rendering**</p>
      <p>
        Create a component that displays "Welcome, User!" if a user is logged in
        (`isLoggedIn` is true) and "Please log in" if not.
      </p>
    </div>
  );
};

export default Test;
